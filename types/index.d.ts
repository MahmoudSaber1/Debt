interface PropsChildren {
    children: React.ReactNode;
}

interface WidgetProps {
    title: string;
    count: string;
    totale?: string;
    style?: string;
    countStyle?: string;
    icon?: React.ReactNode;
}
